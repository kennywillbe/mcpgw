[package]
name = "mcpgw"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true
description = "One binary that manages your MCP servers across every client and gateways their traffic"

[dependencies]
mcpgw-core = { version = "0.1.0", path = "../core" }
clap.workspace = true
anyhow.workspace = true
serde_json.workspace = true
owo-colors.workspace = true
which.workspace = true
tokio.workspace = true
tempfile.workspace = true
ureq.workspace = true
self-replace.workspace = true
sha2.workspace = true
flate2.workspace = true
tar.workspace = true

[target.'cfg(windows)'.dependencies]
zip.workspace = true

[dev-dependencies]
insta.workspace = true
assert_cmd.workspace = true
rmcp.workspace = true
# The self-update end-to-end test builds the release archive it then serves,
# so it needs the same writers the command reads with. Already in the tree,
# so this costs no extra resolution.
flate2.workspace = true
tar.workspace = true
sha2.workspace = true

[target.'cfg(windows)'.dev-dependencies]
zip.workspace = true

[lints]
workspace = true
